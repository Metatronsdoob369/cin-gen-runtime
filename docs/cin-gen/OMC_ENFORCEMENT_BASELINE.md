# OMC Enforcement Baseline (CG-000 + CG-010R + CG-011 refresh)

**Original task:** CG-000 — Map OMC Enforcement Reality for Cin-Gen
**Refresh:** CG-010R — Evidence and commit-scope reconciliation; **CG-011** — capability-derived effects + exact resource bind (repaired rows only)
**Mode (CG-000):** Read-only discovery; no fixes implemented
**OMC root (CG-011 worktree):** `/Users/joewales/NODE_OUT_Master/CG011/open-model-contracts`
**Cin-Gen runtime root (CG-011 worktree):** `/Users/joewales/NODE_OUT_Master/CG011/cin-gen-runtime`
**OMC commit under CG-010R review:** `a317bf86d5ffe01ca01baa4460e1d78498769e55`
**Remote auto-checkpoint:** `b827845371f88e913414e07b16fc383cb96654bb` (`chore(registry): auto-checkpoint a317bf86 [skip ci]`)
**CG-000 HEAD observed (historical):** `f5a38b4b6dedf6a6281e203d8925c569c652df79`
**Evidence:** `docs/evidence/CG-010_SPINE_REPAIR.md`; `docs/evidence/CG-011_CAPABILITY_RESOURCE_SCOPE.md`
**BUILD_STATE:** `currentTask: CG-011`; `completedTasks` includes `CG-010`; `stateChangingAdaptersEnabled: false`

Companion artifacts:

- `docs/cin-gen/OMC_ACTIVE_TOPOLOGY.mmd`
- `docs/cin-gen/OMC_DUPLICATION_REGISTER.md`
- `docs/evidence/CG-010_SPINE_REPAIR.md`
- `docs/evidence/CG-011_CAPABILITY_RESOURCE_SCOPE.md`

---

## Status legend


| Status                | Meaning                                                                                |
| --------------------- | -------------------------------------------------------------------------------------- |
| `ENFORCED_AND_TESTED` | Runtime interception exists and a concrete automated test exercises it                 |
| `ENFORCED_UNTESTED`   | Runtime interception exists; no dedicated automated test found in CI or default `test` |
| `PARTIALLY_ENFORCED`  | Some but not all of the claimed behavior is implemented                                |
| `DOCUMENTED_ONLY`     | Spec/constitution asserts the rule; no active runtime interception                     |
| `STUB`                | Explicit placeholder return or echo                                                    |
| `DISCONNECTED`        | Implementation exists but is not on the mediated execution path that claims it         |
| `DUPLICATE_AUTHORITY` | Two or more active surfaces claim equivalent authority                                 |
| `NOT_FOUND`           | No implementation located outside archived checkpoints                                 |
| `UNKNOWN_BLOCKED`     | Could not verify without mutating scripts, secrets, or external services               |


---

## Enforcement truth table


| Claim                                          | Canonical source                                                                                           | Runtime interception                                                                                                                                                                                                           | Tests                                                               | Status                             | Cin-Gen decision                                                                                         | Evidence                                                                                                                                                          |
| ---------------------------------------------- | ---------------------------------------------------------------------------------------------------------- | ------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------ | ------------------------------------------------------------------- | ---------------------------------- | -------------------------------------------------------------------------------------------------------- | ----------------------------------------------------------------------------------------------------------------------------------------------------------------- |
| Zod payload validation (registry contracts)    | `spec/contracts/index.ts` → `OMC_REGISTRY`; Zod schemas under `spec/contracts/v3/*`                        | Informant `validate_payload` runs `entry.schema.safeParse(payload)` in `src/informant/mcp-server.ts`                                                                                                                           | No CI job invokes Informant validate; root `npm test` is a stub     | `ENFORCED_UNTESTED`                | **REUSE** Informant + registry for shape checks; add negative conformance tests in Phase 1               | `mcp-server.ts` ~230–279; registry header claims Law/runtime separation                                                                                           |
| JSON Schema generation + drift detection       | `scripts/export-json-schema.ts`; committed `spec/json-schema/*.schema.json`                                | CI `.github/workflows/schema-sync.yml` runs `npm run gen:schema` then `git diff --exit-code spec/json-schema/`                                                                                                                 | Workflow itself is the gate; also `checkpoint.yml` / `check:schema` | `ENFORCED_AND_TESTED`              | **REUSE** for any Cin-Gen pack schemas promoted into `OMC_REGISTRY`                                      | `schema-sync.yml` lines 41–53; CG-010 `gen:schema` also filled pre-existing Eve/physics drift (see CG-010R §3)                                                    |
| Contract registry lookup                       | `spec/contracts/index.ts` (`listContracts`, `getContract`)                                                 | Informant `list_contracts` / `get_contract`                                                                                                                                                                                    | No automated Informant tests in CI                                  | `ENFORCED_UNTESTED`                | **REUSE**                                                                                                | Manual registration only — orphan `spec/contracts/v3/script-manifestation.ts` not in registry                                                                     |
| MCP tool enumeration (Informant)               | `src/informant/mcp-server.ts` `ListToolsRequestSchema` handler                                             | Same file; exposes five tools                                                                                                                                                                                                  | Untested in CI                                                      | `ENFORCED_UNTESTED`                | **REUSE** for SAFE observation plane                                                                     | Tools: `list_contracts`, `get_contract`, `read_governance_law`, `validate_payload`, `recall_checkpoint`                                                           |
| Governance-law retrieval                       | Docs under `spec/domicile-governance/admission/*.md`                                                       | Informant `read_governance_law` reads only that admission directory                                                                                                                                                            | Untested in CI                                                      | `PARTIALLY_ENFORCED`               | **EXTEND** if Cin-Gen needs GATES/ADMISSION/OVERVIEW via MCP — today only admission MD files             | Path resolve: `../../spec/domicile-governance/admission/${docId}.md` (`mcp-server.ts` ~203–204); not a general law index                                          |
| SAFE default                                   | `spec/GATES.md`; spine gate + catalog `effectClass`                                                        | SAFE + catalog `STATE_CHANGE`/`IRREVERSIBLE` → DENY. Effect facts from injected catalog (`deriveEffectFacts`), not caller boolean. Bridge/LawCRON still bypass. | CG-011 SAFE+mutating effectClass cases                              | `ENFORCED_AND_TESTED` (local only) | **REUSE** spine; do not claim remote CI or adapter no-bypass                                         | CG-011 evidence; legacy bypasses out of Cin-Gen jurisdiction                                                                                                      |
| ARMED approval                                 | `omc.v3.execution-authorization` **3.1.0** approval fields; GATES intent                                   | Structured approval **metadata** required structurally for ARMED (Zod). **Human/identity verification absent.**                                                    | Incomplete ARMED Zod + execute denial                               | `PARTIALLY_ENFORCED`               | **REUSE** metadata shape; do not claim human approval                                                    | CG-010R §4; CG-011 structural ARMED                                                                                                                               |
| Scope-field presence                           | EA 3.1.0 `actionId` (replaces `scope`)                                                                     | ARMED/SAFE require `actionId` at Zod; missing → schema denial                                                                                                      | Incomplete ARMED Zod tests                                          | `ENFORCED_AND_TESTED` (local only) | **REUSE** structural presence; binding is separate claim                                                 | CG-011                                                                                                                                                            |
| Exact action matching                          | EA `actionId` + catalog resolve                                                                            | Request `actionId` must equal authorization `actionId`; unknown/disabled catalog entries DENY                                                                      | Action mismatch / unknown / disabled cases                          | `ENFORCED_AND_TESTED` (local only) | **REUSE** exact match only; no wildcards                                                                 | CG-011; remote CI still unproven                                                                                                                                  |
| Resource-scope enforcement                     | `ResourceRef` + gate exact equality + declared type membership                                             | Exact `resourceType`/`resourceId` request↔auth match; undeclared type DENY. No glob/regex/hierarchy.                                                               | Resource type/id mismatch + undeclared type tests                   | `ENFORCED_AND_TESTED` (local only) | **REUSE** for Cin-Gen spine; wildcards remain forbidden                                                  | CG-011; not production catalog custody                                                                                                                            |
| Catalog-derived effect classification          | `omc.v3.action-catalog-entry` + `deriveEffectFacts` + injected `ActionCatalog`                             | `stateChanging`/`reversible` derived after catalog resolve; caller effect keys rejected; schema alone ≠ provenance                                                 | deriveEffectFacts + caller-reject + receipt effectClass tests       | `ENFORCED_AND_TESTED` (local only) | **REUSE**; production catalog custody still future                                                       | CG-011 evidence §2                                                                                                                                                |
| ARMED expiry evaluation on the active spine    | `spec/GATES.md` “Expiry Enforced”                                                                          | `gate-evaluation.ts` parses expiry and compares to `now`                                                                                                                 | `governance-orchestrator.test.ts` expired ARMED case                | `ENFORCED_AND_TESTED` (local only) | **REUSE**; wire CI before treating as remote-proven                                                      | CG-010; CG-011 regression                                                                                                                                         |
| Existing specialist/Bridge expiry helpers      | Domicile specialist gate helpers; Bridge escrow session TTL                                                | Date compare exists in `validateSpecialistGate` and `escrow-store.getSession`, but neither intercepts the constitutional ARMED execution path                                                                                  | Bridge/governance-gate tests do not prove GATES ARMED expiry        | `DISCONNECTED`                     | **DEFER** as gate Law; may **REUSE** patterns when wiring the spine — do not cite as current enforcement | `spec/contracts/domicile/specialist.contract.ts` ~227–234; `server/bridge/src/escrow-store.ts` ~25–28                                                             |
| Owner/authority verification                   | `omc.v3.execution-authorization` 3.1.0                                                                     | Non-empty `owner` required structurally for ARMED; no cryptographic identity                                                                                       | Incomplete ARMED Zod / execute denial                               | `ENFORCED_AND_TESTED` (local only) | **EXTEND** later for signed identity; field attribution is metadata only                                 | CG-011                                                                                                                                                            |
| Reversibility declaration                      | `deriveEffectFacts` from catalog `effectClass`                                                             | READ_ONLY → `reversible: null`; STATE_CHANGE → true; IRREVERSIBLE → false. Not caller-supplied. Consult reversible only when `stateChanging === true`.              | deriveEffectFacts unit tests                                        | `ENFORCED_AND_TESTED` (local only) | **REUSE** derived facts; not a separate caller field                                                     | CG-011                                                                                                                                                            |
| Revocation                                     | Implied by time-limited ARMED authority in Cin-Gen plan / GATES prose                                      | No `revoke`/`revocation` implementation in active `.ts`                                                                                                                                                                        | None                                                                | `NOT_FOUND`                        | **BUILD** before long-lived ARMED sessions                                                               | Repo-wide active-path search                                                                                                                                      |
| Core orchestrator audit                        | GATES “Audit Trail”; spine audit sink                                                                      | In-memory receipts + **file-backed JSONL** (`appendFileSync`); default `os.tmpdir()/omc-spine-audit.jsonl` or `OMC_SPINE_AUDIT_LOG`. No integrity/retention policy | `governance-orchestrator.test.ts` durable allow receipt             | `PARTIALLY_ENFORCED`               | **REUSE** for local receipts only; not evidence custody                                                  | CG-010R §7                                                                                                                                                        |
| Bridge JSONL audit                             | PIPELINE Bridge notes; `server/bridge/src/audit-logger.ts`                                                 | Writes JSONL to stdout and file (`OMC_AUDIT_LOG` or temp path) on Bridge routes                                                                                                                                                | Bridge path untested by root CI unit suite                          | `ENFORCED_UNTESTED`                | **REUSE** pattern for world-adapter receipts                                                             | `server/bridge/src/audit-logger.ts`                                                                                                                               |
| Audit relationship to the constitutional spine | Cin-Gen ADR-001 one-surface authority                                                                      | Spine owns allow/deny receipts for Cin-Gen; Bridge/LawCRON still bypass for legacy domains                                                                       | Spine tests emit receipts; Bridge not on Cin-Gen path               | `PARTIALLY_ENFORCED`               | **REUSE** spine for Cin-Gen; Bridge bypass remains out of Cin-Gen jurisdiction                           | ADR-001 + CG-010; no-bypass for *admitted* Cin-Gen adapters still future work                                                                                    |
| MCP freshness                                  | Admission docs (`MCP_ADMISSION_CONTRACT.md` freshness obligations)                                         | No freshness timestamp/TTL enforcement on Informant responses beyond live Zod→JSON Schema generation                                                                                                                           | None                                                                | `DOCUMENTED_ONLY`                  | **DEFER** for SAFE schema reads; **BUILD** for capability snapshots (Phase 2)                            | Admission MD vs Informant handlers                                                                                                                                |
| MCP admission verification                     | `spec/ADMISSION.md`; `spec/OVERVIEW.md` lists `check_mcp_admission`                                        | Tool **absent** from both MCP servers (`src/informant/mcp-server.ts`, `server/src/index.ts`)                                                                                                                                   | None                                                                | `DOCUMENTED_ONLY`                  | **BUILD** admission harness before admitting Unreal MCP                                                  | OVERVIEW diagram ~145; server tool list ~30–102                                                                                                                   |
| Execution mediation / no-bypass                | ADR-001 + CG-010 spine                                                                                     | Cin-Gen authoritative path is spine-only; Bridge/LawCRON still exist as legacy bypasses but are non-authoritative                                                | Spine conformance suite; ADR-001 declaration                        | `PARTIALLY_ENFORCED`               | **REUSE** spine for Cin-Gen; never admit Bridge/LawCRON as Cin-Gen adapters                              | CG-010 evidence note                                                                                                                                              |
| Checkpoint recall                              | Informant tool `recall_checkpoint`                                                                         | Explicit stub JSON; no `registry/checkpoints` read                                                                                                                                                                             | N/A (stub)                                                          | `STUB`                             | **DEFER** for proof-shot; do not pretend recall works                                                    | `mcp-server.ts` ~283–305                                                                                                                                          |
| Lesson memo / checkpoint shipping rules        | README / registry process; `scripts/checkpoint.sh`, `ship.sh`; CI `registry-gate.yml`, `auto-registry.yml` | CI registry-gate requires checkpoint + MANIFEST edit on relevant diffs; `auto-registry.yml` mutates checkpoints on push                                                                                                        | Bash/CI gates, not unit tests                                       | `ENFORCED_UNTESTED` (process gate) | **REUSE** process; **never run** mutating scripts from Cin-Gen builders without human review             | Listed scripts not executed this task; `.husky/pre-commit` → `validate-registry.ts`                                                                               |
| Error and denial receipts                      | Spine `SpineReceipt`                                                                                       | Allow/deny receipts with reason, receiptId, mediated, worldMutated:false                                                                                         | Negative suite in `governance-orchestrator.test.ts`                 | `ENFORCED_AND_TESTED` (local only) | **REUSE** on spine; server MCP stubs still success-shaped                                                | CG-010; CG-010R §9                                                                                                                                                |
| Gate-decision mediation                        | CG-010 / ADR-001 spine                                                                                     | SAFE/ARMED evaluate → allow/deny `SpineReceipt` on `executeContract`                                                                                             | Positive + negative cases in `governance-orchestrator.test.ts`      | `ENFORCED_AND_TESTED` (local only) | **REUSE** as Cin-Gen mediation entry                                                                     | CG-010R §6                                                                                                                                                        |
| World execution via spine                      | CG-010 mediation contract                                                                                  | No Unreal/Bridge/disk/git dispatch on allow path; returns `mediated: true`, `worldMutated: false`                                                                | Allow-path asserts non-mutation only                                | `NOT_FOUND`                        | Honest non-mutation ≠ world execution; adapters remain separate                                          | CG-010R §6                                                                                                                                                        |
| Escrow “Zod” validation                        | `src/escrow/validatePayload.ts` comments claim Zod                                                         | Hand-rolled required-key check; `RegisterIssue` is console stub; catch path logs “Governance Not Hooked”                                                                                                                       | Pack/UI tests exist but not root CI                                 | `PARTIALLY_ENFORCED`               | **DEFER** for Cin-Gen (Roblox-specific); do not reuse as universal Law                                   | `validatePayload.ts` ~103–155, ~126                                                                                                                               |
| Domain pack → Informant visibility             | `packs/` precedent; constitution phase-gates reference pack schemas                                        | No Informant/`OMC_REGISTRY` loader for packs                                                                                                                                                                                   | None                                                                | `DOCUMENTED_ONLY`                  | **BUILD** pack registration path for `packs/cin-gen` (withheld until after CG-000/001)                   | Grep: zero `packs/` refs in `src/informant/**`                                                                                                                    |
| Root default test suite                        | OMC `package.json` `"test"` → `test:governance`                                                            | Local suite is CG-011 spine tests (23/23 on builder). **Remote CI still not claimed.**                                                                              | 23/23 builder-local only                                            | `PARTIALLY_ENFORCED`               | **EXTEND** — wire workflow to `test:governance` before remote proof                                  | CG-011 evidence                                                                                                                                                   |
| Pinecone / dream-cycle / Postgres governance   | `spec/CONTRACTS.md`, `spec/GOVERNANCE.md`, `spec/OVERVIEW.md` tool list                                    | No matching runtime in `src/` or `server/src`                                                                                                                                                                                  | None                                                                | `DOCUMENTED_ONLY`                  | **DEFER** — do not design Cin-Gen against these claims                                                   | Spec-only surfaces                                                                                                                                                |


---

## Active execution surfaces (state-changing)


| Surface                                            | Changes state?                                        | SAFE/ARMED mediation?                                           | Cin-Gen stance                                            |
| -------------------------------------------------- | ----------------------------------------------------- | --------------------------------------------------------------- | --------------------------------------------------------- |
| `src/informant/mcp-server.ts`                      | No (read/validate)                                    | Declared SAFE                                                   | Primary reuse candidate for Law observation               |
| `server/src/core/orchestrator.ts`                  | No world mutation (`worldMutated: false`)             | Gate-decision mediation: Law + SAFE/ARMED evaluate              | Mediation entry for Cin-Gen; not world execution          |
| `server/src/index.ts` MCP                          | No real dispatch (returns `dispatched` / `initiated`) | Heuristic `check_gate` only                                     | Do not treat as governance server of record               |
| `server/bridge` HTTP (`/escrow`, `/submit`, …)     | Yes (memory escrow, disk, git)                        | Escrow integrity + optional submit auth; not GATES orchestrator | Roblox-era adapter; not Cin-Gen Unreal path               |
| `packs/roblox-game-automator` LawCRON orchestrator | Yes (files, HTTP escrow, `rojo`)                      | Own policies; hardcodes empty approvals                         | Out of Cin-Gen critical path                              |
| `boxstar-omc-bootstrap/bridge`                     | Yes (duplicate Bridge)                                | Same class as `server/bridge`                                   | Historical/bootstrap duplicate — see duplication register |


---

## CI vs claimed enforcement


| Workflow                  | Actually verifies                                          |
| ------------------------- | ---------------------------------------------------------- |
| `schema-sync.yml`         | Zod → JSON Schema drift                                    |
| `registry-gate.yml`       | Diff provenance (checkpoint + MANIFEST) for certain paths  |
| `checkpoint.yml`          | `scripts/validate-registry.ts` + schema check              |
| `auto-registry.yml`       | **Mutates** registry checkpoints (do not trigger casually) |
| `sovereign-sync-cron.yml` | Shallow Luau check + `server/bridge` typecheck/verify      |


Root `npm test` / `test:governance`, `test:ui`, and pack test scripts are **not** invoked by remote CI on `a317bf86` (CG-010R). Local suite evidence only until a workflow wires them.

---

## Prioritized REUSE / EXTEND / REPAIR / BUILD / DEFER matrix


| Priority | Decision   | Item                                                                                   | Rationale                                                                  |
| -------- | ---------- | -------------------------------------------------------------------------------------- | -------------------------------------------------------------------------- |
| P0       | **REUSE**  | `spec/contracts/index.ts` + Informant SAFE tools                                       | Only coherent Law→MCP observation spine                                    |
| P0       | **REUSE**  | `gen:schema` + `schema-sync.yml`                                                       | Real drift gate with CI evidence                                           |
| P0       | **REPAIR** | Wire remote CI to `test:governance`                                                    | Local CG-011 suite 23/23; remote CI still unproven                         |
| P0       | **EXTEND** | Production catalog custody + identity-bound approval (beyond metadata)                 | Injected catalog trusted by construction in tests; approval still metadata |
| P1       | **REPAIR** | Spine JSONL integrity/retention (or honestly scope temp-path sink as local-only)       | Default `os.tmpdir()` file; no hash chain or custody policy                |
| P1       | **BUILD**  | MCP admission check for candidate Unreal MCP                                           | Documented tool missing                                                    |
| P1       | **EXTEND** | Informant law retrieval beyond admission MD                                            | Needed for GATES/charter inspectability                                    |
| P2       | **BUILD**  | Domain-pack registration so Cin-Gen pack appears in `OMC_REGISTRY`/Informant           | Packs are invisible today                                                  |
| P2       | **DEFER**  | `recall_checkpoint` full implementation                                                | Explicit stub; not required for first proof-shot                           |
| P2       | **DEFER**  | Escrow Lua-bridge validator as universal Law                                           | Sector-specific; shallow fake-Zod                                          |
| P3       | **DEFER**  | Pinecone, dream-cycle, Postgres TARS governance claims                                 | Spec-only; not active spine                                                |
| —        | **DEFER**  | Running `checkpoint.sh` / `ship.sh` / auto-registry from Cin-Gen tasks                 | Mutating; out of CG-000                                                    |


---

## Lesson memo prompt (answered)

> Which OMC mechanisms were genuinely executable, which were primarily
> constitutional intent, and what is the smallest repair that would let
> Cin-Gen trust the existing spine without redesigning it?

**Genuinely executable today:** Zod registry plus Informant
`list/get/validate`; JSON Schema export and CI drift detection; Bridge escrow
integrity, TTL, and JSONL audit within its Roblox adapter lane; registry
provenance CI; **local** spine gate-decision mediation (Law parse, date expiry,
exact actionId + exact resourceRef bind, catalog-derived effect facts,
structured approval metadata, SAFE+mutating effectClass denial, allow/deny
receipts) — not yet proven in remote CI.

**Primarily constitutional intent / still open:** Human/identity-verified
approval; production catalog custody/provenance; world execution via spine;
revocation; MCP admission verification; no-bypass for *admitted* adapters;
checkpoint recall; freshness SLOs; production-grade audit custody.

**Smallest next repair (post-CG-011):** Wire CI to `test:governance`; keep
Bridge, LawCRON, and `server/src/index.ts` outside Cin-Gen’s authoritative
execution path. Do not enable `stateChangingAdaptersEnabled` until catalog
custody and adapter mediation are ratified.

---

## Known limitations of this audit

- The CG-000 pass did not execute Informant or Bridge processes.
- It did not run schema-generation or registry-mutating scripts from Cin-Gen.
- It did not deeply inspect `domicile_live`.
- Build-plan snapshot `001b3c3f…` was absent; mapping used `f5a38b4b`.
- CG-010R reconciles claims against `a317bf86` / `b827845` without changing OMC
  source or marking CG-010 complete in `BUILD_STATE.json`.
- Final product authority between Informant v3 and `server/src/index.ts` v0.2
  still requires explicit human ratification.

